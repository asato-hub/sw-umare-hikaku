# AGENTS.md

## ユーザーについて

ユーザーはプログラミング初心者です。

高校のプログラミングを修了している程度の知識を前提にしてください。

そのため、作業報告や説明では以下を守ってください。

* 専門用語を使いすぎない
* 専門用語を使う場合は短く補足する
* 変更内容を初心者にも分かるように説明する
* 動作確認方法を具体的に書く
* 一度に大きく作りすぎない

## プロジェクトの目的

このプロジェクトは、ゲーム「ソードワールド」の種族ごとの能力値傾向を、レーダーチャートで可視化するためのものです。

汎用的なWebアプリを作ることが目的ではありません。

今回用意されたExcelファイルから、一度だけ集計・可視化結果を作ることが目的です。

Excelデータは今後変化しない前提です。

## 入力ファイル

元データは以下です。

```text
data/生まれ表一覧.xlsx
```

このExcelには、種族ごとの「生まれ」と能力値データが含まれています。

## 重要な方針

ブラウザ上でExcelを毎回読み込む仕組みは優先しません。

まずはExcelを一度だけ解析し、アプリで使いやすい集計済みデータを作成してください。

基本方針は以下です。

```text
Excel解析
↓
種族ごとの集計データ作成
↓
集計結果の確認
↓
集計済みデータからグラフ表示
```

## 扱う能力値

最初に扱う能力値は以下の6つです。

* 器用
* 敏捷
* 筋力
* 生命力
* 知力
* 精神力

## ステータス計算ルール

このプロジェクトでは、能力値を単一の値ではなく、以下の3種類で扱います。

* 最小値
* 平均値
* 最大値

## ダイスの扱い

### 2d

`2d` は以下の値として扱います。

* 最小値: 2
* 平均値: 7
* 最大値: 12

### 1d

`1d` は以下の値として扱います。

* 最小値: 1
* 平均値: 3.5
* 最大値: 6

## 計算例

```text
5 + 2d
```

という能力値の場合、以下のように計算します。

```text
最小値 = 5 + 2 = 7
平均値 = 5 + 7 = 12
最大値 = 5 + 12 = 17
```

```text
3 + 1d
```

という能力値の場合、以下のように計算します。

```text
最小値 = 3 + 1 = 4
平均値 = 3 + 3.5 = 6.5
最大値 = 3 + 6 = 9
```

## 種族ごとの集計ルール

各種族には複数の「生まれ」があります。

種族全体の傾向を見るため、同じ種族に属するすべての「生まれ」を同じ重みで平均してください。

重みづけは行いません。

集計方法は以下です。

```text
種族の最小値 = その種族に属する各生まれの最小値の平均
種族の平均値 = その種族に属する各生まれの平均値の平均
種族の最大値 = その種族に属する各生まれの最大値の平均
```

## 目標UI

最終的には、添付画像のようなレーダーチャート風のUIを目指します。

1つの種族について、以下の3本の線を重ねて表示します。

* 最小値ライン
* 平均値ライン
* 最大値ライン

これにより、種族ごとの能力傾向と、ダイスによるばらつきの大きさを視覚的に確認できるようにします。

## 最初の作業でやること

最初の作業では、完成版UIを作らないでください。

まずは以下を優先してください。

1. Excelファイルを読み取る
2. シート一覧を確認する
3. 使用するシートを確認する
4. 種族名、生まれ、能力値を抽出する
5. ダイス表記を最小・平均・最大に変換する
6. 種族ごとの集計済みデータを作成する
7. 集計結果を表で確認できるようにする
8. JSONデータとして保存する

## 期待する出力

可能であれば、以下のファイルを作成してください。

```text
data/race-stats.json
```

種族ごとの集計済みデータです。

```text
docs/aggregation-report.md
```

集計ルール、読み取り結果、判断に迷った点などをまとめたレポートです。

```text
index.html
style.css
script.js
```

集計結果を確認するための最小表示画面です。

最初の段階では、美しいUIよりも、集計結果の正しさを優先してください。

## 禁止事項

* 最初から完成版UIを作らない
* Excelを毎回ブラウザで読み込む仕組みを優先しない
* 複雑なバックエンドやデータベースを導入しない
* いきなり大きなフレームワークを導入しない
* 集計ルールが曖昧な箇所を勝手に断定しない
* 動いている機能を大きく壊すような変更をしない

## 作業報告のルール

作業後は、必ず以下を報告してください。

1. 変更したファイル
2. 何を変更したか
3. どのように確認すればよいか
4. 判断に迷った点
5. 次にやるとよい作業

初心者にも分かるように説明してください。
 
## ダイス値は設定として扱う

`2d` や `1d` の最小・平均・最大値は、コード内に直接固定で書き込まないでください。

必ず設定として分離してください。

例：

```js
const diceConfig = {
  "2d": {
    min: 2,
    avg: 7,
    max: 12
  },
  "1d": {
    min: 1,
    avg: 3.5,
    max: 6
  }
};

将来的に、グラフ表示を見やすくするため、2d を以下のように変更する可能性があります。

const diceConfig = {
  "2d": {
    min: 5,
    avg: 7,
    max: 9
  },
  "1d": {
    min: 2,
    avg: 3.5,
    max: 5
  }
};

このような変更が、集計処理全体の書き換えなしで行えるようにしてください。

正式値と表示用値

可能であれば、以下の2種類を分けて扱ってください。

ruleDiceConfig: ルール上の正式なダイス範囲
displayDiceConfig: グラフ表示用のダイス範囲

最初は両方とも正式値で構いません。

ただし、後から表示用だけ 2d = 5 / 7 / 9 のように変更できる構造にしてください。

グラフや集計表には、どのダイス設定を使っているか分かるようにしてください。